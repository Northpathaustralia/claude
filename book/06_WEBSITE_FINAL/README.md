# Website — Nothing Exists Here to Continue

**Honest status:** there is no Wix site, no website build, and no
website files anywhere in this repository or any of its branches. I
checked every branch again before writing this folder.

The instruction to "continue the existing website rather than
restarting it" assumes a prior build that I cannot find. This may be
a separate project (for example, built directly inside Wix's own
editor/AI assistant, which is a different tool from this one) that
isn't connected to this workspace.

**I have not built a new website in its place**, because the
instructions were explicit: continue, don't restart. Building a new
one here would be restarting, under a different name.

## What I need from you to actually help with the website
- Confirm whether a Wix site already exists, and if so, share the
  site URL or Wix site ID so I can check what's really there before
  touching anything
- Or confirm this is genuinely a fresh start, so a real "build from
  scratch" can be scoped honestly (as new work, not a continuation)

## What's ready whenever the website work does start
See `WIX_HANDOVER_NOTE.md` (repository root of `book/`) and
`07_WEB_MARKETING_ASSETS/` in this folder set — cover image, preview
pages, character reference images, and description copy, all pulled
from the now-complete approved book.
